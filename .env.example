# IALAW Digital Lawyers — Constructor de prompts jurídicos
#
# Esta aplicación NO requiere variables de entorno para funcionar.
# Toda la lógica se ejecuta en el navegador (cliente).
# No hay base de datos, no hay APIs de IA, no hay servicios externos.
#
# Este archivo se mantiene solo como referencia por si más adelante
# se añade telemetría o analytics opcionales.
